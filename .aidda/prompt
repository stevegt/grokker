x/tokencount: add -l flag to list all countable files

Add a -l flag to list the relative path of all files that are counted
by tokencount, one per line.  This is useful for feeding the file list
into other tools.

In:
    x/tokencount/main.go
Out: 
    x/tokencount/main.go
.stop

In: 
    v3/aidda/aidda.go

Sysmsg header missing, using default.Sysmsg
